#include <stdio.h>
int main(){
	int n;
	int cnt=0;
	scanf("%d", &n);
	if( n!=1 ){
		for(; n!=1; cnt++){
			if( n%2==0 ){
				n= n/2  ;
			}else
				n= (3*n+1)/2;
		}
	}
	printf("%d\n", cnt);
	return 0;
}
